import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Firebase is initialized at runtime from env vars.
  // Production builds succeed without secrets (see README).
};

export default nextConfig;
